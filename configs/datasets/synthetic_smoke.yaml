# SYNTHETIC pipeline-test data — NOT a research dataset.
#
# Generated by:  python -m saryolo synth-data --out datasets/processed/synthetic_smoke
#
# Used by the `_smoke_*.yaml` experiment configs to prove the training and
# evaluation pipeline runs end to end on a CPU, with no download and no GPU.
# Metrics from this data must never appear in a paper or a results table.
path: ../../datasets/processed/synthetic_smoke
train: images/train
val: images/val
test: images/test
nc: 1
names:
  - target
