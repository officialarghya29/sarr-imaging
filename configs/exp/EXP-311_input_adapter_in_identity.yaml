# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Module ablation -- input adapter slot, arm 1/4 (in_identity).
experiment:
  id: EXP-311
  name: 'input adapter: in_identity'
  description: Module ablation -- input adapter slot, arm 1/4 (in_identity).
model: ../models/yolo11s_in_identity.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Module ablation -- input adapter slot, arm 1/4 (in_identity).
