# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# multi-seed seed 1
experiment:
  id: EXP-012
  name: Multi-seed validation (seed 1)
  description: Repeat of EXP-007 with a different seed.
model: ../models/yolo11s_full_s.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 1
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: EXP-007 repeated with seed 1; EXP-012 reports mean +/- std over all seeds.
