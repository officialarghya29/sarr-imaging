# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Module ablation -- conditioning slot, arm 6/8 (cond_continuous).
experiment:
  id: EXP-336
  name: 'conditioning: cond_continuous'
  description: Module ablation -- conditioning slot, arm 6/8 (cond_continuous).
model: ../models/yolo11s_cond_continuous.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Module ablation -- conditioning slot, arm 6/8 (cond_continuous).
