# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Phase 3 init: SAR-pretrained checkpoint fine-tuned on SAR (e.g. MSFA-style weights); state the path before running
experiment:
  id: EXP-403
  name: RGB/SAR pretraining baseline (SAR-pretrained checkpoint fine-tuned on SAR)
  description: 'Phase 3: SAR-pretrained checkpoint fine-tuned on SAR (e.g. MSFA-style
    weights); state the path before running'
model: ../models/yolo11s_baseline.yaml
dataset: ../datasets/ssdd.yaml
init:
  weights: PATH/TO/msfa_or_sar_checkpoint.pt
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Phase 3 init comparison; shared config with EXP-401..403 except the init stage.
  SAR-pretrained checkpoint fine-tuned on SAR (e.g. MSFA-style weights); state the
  path before running
