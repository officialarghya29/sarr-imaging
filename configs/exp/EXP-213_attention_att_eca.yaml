# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Module ablation -- attention slot, arm 3/6 (att_eca).
experiment:
  id: EXP-213
  name: 'attention: att_eca'
  description: Module ablation -- attention slot, arm 3/6 (att_eca).
model: ../models/yolo11s_att_eca.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Module ablation -- attention slot, arm 3/6 (att_eca).
