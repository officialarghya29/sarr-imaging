# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Phase 3 init: random init (fresh build from the model YAML; the Phase-3 control)
experiment:
  id: EXP-401
  name: RGB/SAR pretraining baseline (random init)
  description: 'Phase 3: random init (fresh build from the model YAML; the Phase-3
    control)'
model: ../models/yolo11s_baseline.yaml
dataset: ../datasets/ssdd.yaml
init: none
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Phase 3 init comparison; shared config with EXP-401..403 except the init stage.
  random init (fresh build from the model YAML; the Phase-3 control)
