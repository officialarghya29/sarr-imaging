# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Engineering baseline; every other row is measured against this.
experiment:
  id: EXP-001
  name: YOLO baseline
  description: Engineering baseline; every other row is measured against this.
model: ../models/yolo11s_baseline.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
evaluation_only: false
notes: Engineering baseline; every other row is measured against this.
