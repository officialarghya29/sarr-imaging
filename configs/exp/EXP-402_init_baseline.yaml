# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Phase 3 init: COCO-pretrained yolo11s.pt fine-tuned on SAR (the standard route)
experiment:
  id: EXP-402
  name: RGB/SAR pretraining baseline (COCO-pretrained yolo11s.pt fine-tuned on SAR)
  description: 'Phase 3: COCO-pretrained yolo11s.pt fine-tuned on SAR (the standard
    route)'
model: ../models/yolo11s_baseline.yaml
dataset: ../datasets/ssdd.yaml
init: coco11
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Phase 3 init comparison; shared config with EXP-401..403 except the init stage.
  COCO-pretrained yolo11s.pt fine-tuned on SAR (the standard route)
