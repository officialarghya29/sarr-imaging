# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Module ablation -- conditioning slot, arm 8/8 (cond_spatial).
experiment:
  id: EXP-338
  name: 'conditioning: cond_spatial'
  description: Module ablation -- conditioning slot, arm 8/8 (cond_spatial).
model: ../models/yolo11s_cond_spatial.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Module ablation -- conditioning slot, arm 8/8 (cond_spatial).
