# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Isolates the loss (Component 7) from the architecture.
experiment:
  id: EXP-008
  name: SAR-YOLO w/o SAR loss
  description: Isolates the loss (Component 7) from the architecture.
model: ../models/yolo11s_full_noloss.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
evaluation_only: false
notes: Isolates the loss (Component 7) from the architecture.
