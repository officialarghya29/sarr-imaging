# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Module G: the target prior chooses the radial frequency bands. Added rather than folded into EXP-017 so the ladder row and the slot study measure the same graph.
experiment:
  id: EXP-018
  name: + prior-conditioned spectral selection
  description: 'Module G: the target prior chooses the radial frequency bands. Added
    rather than folded into EXP-017 so the ladder row and the slot study measure the
    same graph.'
model: ../models/yolo11s_v2_prior_spectral.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
evaluation_only: false
notes: 'Module G: the target prior chooses the radial frequency bands. Added rather
  than folded into EXP-017 so the ladder row and the slot study measure the same graph.'
