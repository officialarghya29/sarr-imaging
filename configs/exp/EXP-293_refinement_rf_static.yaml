# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Module ablation -- refinement slot, arm 3/4 (rf_static).
experiment:
  id: EXP-293
  name: 'refinement: rf_static'
  description: Module ablation -- refinement slot, arm 3/4 (rf_static).
model: ../models/yolo11s_rf_static.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Module ablation -- refinement slot, arm 3/4 (rf_static).
