# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# The SEC. 4 term switched on: identical graph to EXP-017, different objective. Its own experiment because a loss cannot be ablated by removing parameters.
experiment:
  id: EXP-019
  name: FULL v2 + representation consistency (SEC. 4)
  description: 'The SEC. 4 term switched on: identical graph to EXP-017, different
    objective. Its own experiment because a loss cannot be ablated by removing parameters.'
model: ../models/yolo11s_v2_cons.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
evaluation_only: false
notes: 'The SEC. 4 term switched on: identical graph to EXP-017, different objective.
  Its own experiment because a loss cannot be ablated by removing parameters.'
