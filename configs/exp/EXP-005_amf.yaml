# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Component 4 on top of Components 1-3.
experiment:
  id: EXP-005
  name: + Adaptive Multi-Scale Fusion
  description: Component 4 on top of Components 1-3.
model: ../models/yolo11s_amf.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
evaluation_only: false
notes: Component 4 on top of Components 1-3.
