# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# All modules + P2 head + SAR-aware loss.
experiment:
  id: EXP-007
  name: FULL SAR-YOLO
  description: All modules + P2 head + SAR-aware loss.
model: ../models/yolo11s_full.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
evaluation_only: false
notes: All modules + P2 head + SAR-aware loss.
