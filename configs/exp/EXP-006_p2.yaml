# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# High-resolution detection level, justified by dataset stats.
experiment:
  id: EXP-006
  name: + P2 small-object head
  description: High-resolution detection level, justified by dataset stats.
model: ../models/yolo11s_p2.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
evaluation_only: false
notes: High-resolution detection level, justified by dataset stats.
