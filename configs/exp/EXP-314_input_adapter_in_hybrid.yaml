# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Module ablation -- input adapter slot, arm 4/4 (in_hybrid).
experiment:
  id: EXP-314
  name: 'input adapter: in_hybrid'
  description: Module ablation -- input adapter slot, arm 4/4 (in_hybrid).
model: ../models/yolo11s_in_hybrid.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
notes: Module ablation -- input adapter slot, arm 4/4 (in_hybrid).
