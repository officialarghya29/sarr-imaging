# GENERATED by scripts/make_exp_configs.py -- edit the generator, not this file.
# Uses the EXP-007 checkpoint; no separate training is needed.
experiment:
  id: EXP-010
  name: Efficiency study
  description: Uses the EXP-007 checkpoint; no separate training is needed.
model: ../models/yolo11s_full.yaml
dataset: ../datasets/ssdd.yaml
train:
  epochs: 100
  imgsz: 640
  batch: 16
  seed: 0
  optimizer: auto
  cos_lr: true
  patience: 30
  workers: 8
  deterministic: true
evaluation_only: true
notes: Uses the EXP-007 checkpoint; no separate training is needed.
