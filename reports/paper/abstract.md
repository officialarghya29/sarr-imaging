# Abstract — skeleton

> **Status: BLOCKED-ON-RUN.** The abstract is written last and may contain only
> measured results. Nothing below the line is quotable until the SSDD LOSO runs
> have ledger rows.

---

**Structure to fill** (four sentences, CVPR abstract shape):

1. *Problem.* SAR object detectors degrade when the acquisition changes — but
   the community's responses have been module insertions (frequency, wavelet,
   attention), not a diagnosis of *why* the degradation happens.
2. *Evidence.* [BLOCKED-ON-RUN: the measured cross-sensor / LOSO gap, and the
   probe result that features cluster by acquisition.]
3. *Mechanism.* A metadata-conditioned adapter — [BLOCKED-ON-RUN: which fields
   survive the withholding ablation] — at <0.5% parameter overhead.
4. *Result.* [BLOCKED-ON-RUN: LOSO improvement ± std over seeds, in-domain
   parity, and the capacity-matched control.]

**Rule:** if sentence 4 cannot be written from the ledger, the honest abstract
is a benchmark/analysis abstract built from sentences 1–2 — not a padded one.
